import { ScrollHideDirective } from './hide-header.directive';

describe('ScrollHideDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrollHideDirective();
    expect(directive).toBeTruthy();
  });
});
