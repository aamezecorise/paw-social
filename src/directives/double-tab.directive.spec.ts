import { DoubleTabDirective } from './double-tab.directive';

describe('DoubleTabDirective', () => {
  it('should create an instance', () => {
    const directive = new DoubleTabDirective();
    expect(directive).toBeTruthy();
  });
});
